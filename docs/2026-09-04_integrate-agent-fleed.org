#+TITLE: Integrate the agent fleet into the NixOS repository
#+DATE: [2026-09-04 Fri]
#+TODO: TODO WAIT | DONE CANCEL

* Goal

Move the complete agent-fleet setup into =/etc/nixos/agent-fleet=. Preserve its
reachable Git history. Make the main NixOS definitions the source of truth for
behavior that both setups share.

Keep the Terraform configuration and current local runtime state. Do not apply
Terraform changes, deploy a host, or switch a local system as part of this
work.

* Starting point

- The destination is =/etc/nixos= on branch =main=. It was seven commits ahead
  of =origin/main= at the first inspection. The remote advanced during the
  initial plan commit, so it is now one commit ahead.
- The destination has two unrelated untracked files:
  =remote-need-airy.tsv= and =remote-need-airy.txt=. This work must not stage,
  edit, or remove them.
- The source is =/home/robert/projects/ai/agent-fleet= on branch =main= at
  commit =94eaad3=. It has 47 reachable commits, no tags, and no configured
  remote.
- The source has no tracked changes. Its ignored local state includes
  =.direnv=, =.env=, =.terraform=, =known_hosts=, =result=, and Terraform state
  files.
- The ignored =.terraform= provider cache is about 25 MB. It will remain local
  and untracked after the move.
- The source tracks =ssh/id_ed25519= and =nix/agent-files-r2.env=. They contain
  a private key and R2 credentials. The source stores both files in plaintext.

* Working rules

- Make the initial plan its own commit.
- Import history from a temporary clone. Do not rewrite either existing
  repository in place.
- Preserve source commit authors, dates, messages, and topology. Record the old
  and filtered head commits and compare the reachable commit counts.
- Keep ignored local files ignored. Preserve their paths, file modes, and
  symlinks without reading or printing secret values.
- Use one focused commit for each deduplication change. Include the matching
  progress update in this document in that commit when practical.
- Mark a task =DONE= only after its checks pass. Record failed checks and other
  issues under the task where they occurred.
- Use the existing =just= recipes and Nix development shell for checks.
- Do not stage or modify unrelated work in the destination.
- Keep the old checkout until the integrated setup passes all checks. Removing
  it is a separate destructive action.

* Plan

** DONE 1. Confirm the secret and old-checkout policy
CLOSED: [2026-09-04 Fri 08:41]

- [X] Remove =ssh/id_ed25519= from every imported commit.
- [X] Move the current key to =shared/secrets/agent-fleet-ssh.key= and replace
  all private-key usage with that path.
- [X] Let the existing =*.key= Git attribute encrypt the moved key.
- [X] Remove =nix/agent-files-r2.env= from every imported commit and reuse
  =shared/secrets/agent-files-r2.env=.
- [X] Keep ignored secret-bearing state local and untracked.
- [X] Retain the old checkout after validation unless the user asks to remove
  it.

The Gitleaks history scan found the private key and two R2 credential fields.
It found no other secret in the 47 reachable commits. The ignored =.env= holds
the Hetzner token and an SSH-related value. The ignored Terraform state holds
resource IDs and public addresses. The integration will copy these ignored
files without adding them to Git.

** DONE 2. Record a reproducible import baseline
CLOSED: [2026-09-04 Fri 08:41]

- [X] Record the source and destination head commits, branches, worktree
  states, remotes, tags, and reachable commit counts.
- [X] Record tracked source paths and ignored local paths without reading
  secrets.
- [X] Check references to the old absolute source path so later path changes
  have a complete owner list.
- [X] Note any change from the starting point before the import starts.

Baseline:

- Destination head: =cb09fd5d97ee08ceef2cc4cb9976c685942217e0=.
- Destination tree: =981057f9f5bc63f222590b3e446fe6094ccd0d1e=.
- Destination =origin/main=: =8e4d22a942e033114379e14b8bc65bbfdb10399e=.
- Destination history: 605 commits. Branch =main= is one commit ahead of its
  remote.
- Source head: =94eaad34053a11485f113687673c1e8e9efad37d=.
- Source tree: =107fd6fecb6811d1983e4104318abc40b391931a=.
- Source history: 47 commits from root
  =306bf48a91118dd31f75e7d930ca051d26af1aad=. It has no tags or remotes.
- Source commit metadata signature:
  =7a005f88bf0f299ff87c1dfb73543472ea7a18916bd5d47df27232b1e84bfca4=.
- Source topology signature:
  =5af46e337558ba2f36349707d74fce0b498c4351205a9de46a31dd3f547b5520=.
- Approved source tree signature after secret removal:
  =5d812090c3fdba0f86bea617c4aa800531c0afb55e31d7d3e638d68e49f7ae72=.
  It covers 24 paths.
- Ignored local entries: =.direnv=, =.env=, =.terraform=, =known_hosts=,
  =result=, =terraform.tfstate=, and =terraform.tfstate.backup=.
- The old absolute path occurs in the Moshi copy note, the Codex trusted-project
  list, and the agent-fleet update instructions.

During the plan commit, another process pushed the seven earlier local commits
and advanced =origin/main= from =184c7c8= to =8e4d22a=. No file in the worktree
changed.

Completion checks:

- =remote-need-airy.tsv= remains untracked with SHA-256
  =0e316a71b921351d92ffb7684b3eedcf39e00df04985c1901bc7fb265299e696=.
- =remote-need-airy.txt= remains untracked with SHA-256
  =e3166c9cfee1612a46901845621dfc2259f175ebc0c88f8e29d2e98a088c3b7b=.
- The source worktree remains unchanged.
- The baseline provides enough detail to verify the filtered result.

** DONE 3. Rewrite a disposable copy of the agent-fleet history
CLOSED: [2026-09-04 Fri 08:42]

- [X] Clone the source into a temporary directory on the same machine.
- [X] Run =git filter-repo= there to prefix every source path with
  =agent-fleet/=.
- [X] Remove both confirmed secret files from history in the same disposable
  repository.
- [X] Keep the filter-repo commit map until the import checks pass.
- [X] Compare commit count, topology, authors, dates, messages, and the current
  tracked tree with the source baseline.

Results:

- Filtered head: =3c5e7a5d8b3c4f890ac687ea7f105786c563ca54=.
- Filtered tree: =8877650d9187cae1290f64bdbfa3ab69f9a59b82=.
- The commit map contains all 47 source commits. The filtered history has one
  root and no tags, like the source.
- The metadata, topology, and approved tree signatures match the baseline.
- All 24 approved paths start with =agent-fleet/=.
- Neither removed secret path occurs in the filtered history.
- Gitleaks scanned the 47 filtered commits and found no secret.
- =git fsck --full --strict= passes.

Completion checks:

- All expected source commits remain reachable from the filtered branch.
- Every tracked path is under =agent-fleet/=.
- The filtered current tree matches the approved 24-path source tree.
- The original source remains clean at
  =94eaad34053a11485f113687673c1e8e9efad37d=.

** TODO 4. Merge the filtered history and restore local state

- [ ] Fetch the filtered branch into =/etc/nixos= under a temporary local ref.
- [ ] Merge it into =main= with unrelated histories allowed.
- [ ] Copy the source worktree into =/etc/nixos/agent-fleet= without its =.git=
  directory.
- [ ] Preserve all ignored local state, including =.terraform=, Terraform state,
  =.env=, =.direnv=, =known_hosts=, and =result=.
- [ ] Copy the private key to =shared/secrets/agent-fleet-ssh.key=, replace its
  old uses, and add it through git-crypt.
- [ ] Reuse =shared/secrets/agent-files-r2.env= and replace its fleet use.
- [ ] Ignore the two removed source paths to prevent accidental reintroduction.
- [ ] Verify that Git still ignores local files and that staging contains no
  secret or generated file.
- [ ] Update this task before creating the merge commit.

Completion checks:

- =git log= can traverse both the original NixOS history and the filtered
  agent-fleet history.
- The integrated tracked tree matches the approved filtered tree.
- The integrated local tree contains the ignored source files with the same
  modes and symlink targets.
- The earlier untracked destination files remain unchanged.

** TODO 5. Validate the unchanged integrated agent fleet

- [ ] Enter the agent-fleet development shell from its new path.
- [ ] Run =just fmt= only when a format check requires changes.
- [ ] Run =just check= from =/etc/nixos/agent-fleet=.
- [ ] Check that the OpenTofu commands can use the copied local state and
  provider cache without applying a plan.
- [ ] Check that each host configuration still evaluates from the new path.
- [ ] Check operational recipes for stale absolute or relative paths.

Completion checks:

- OpenTofu formatting and validation pass.
- Every agent-fleet NixOS configuration evaluates.
- The checks do not change infrastructure, hosts, or the local system.

** TODO 6. Inventory duplicated behavior and choose one owner for each rule

- [ ] Compare flake inputs, overlays, package sources, and lock-file ownership.
- [ ] Compare core Nix and NixOS settings, caches, users, SSH, Tailscale, and
  Syncthing.
- [ ] Compare Home Manager settings, development tools, shells, editors, and AI
  agent setup.
- [ ] Compare Moshi, CLIProxyAPI, agent-files, and other local modules or
  scripts with the main repository.
- [ ] Distinguish identical rules from machine-specific differences.
- [ ] Add one concrete TODO task to this document for each proven group of
  duplicated rules. State the main-repository owner and the fleet-specific
  behavior that must remain.
- [ ] Commit the inventory before changing shared behavior.

Completion checks:

- Every proposed deletion has a named replacement in the main repository.
- Every intentional difference has a reason and one owner.
- The inventory contains no speculative abstractions or unrelated cleanup.

** TODO 7. Deduplicate shared Nix inputs and package definitions

- [ ] Replace confirmed duplicate inputs, overlays, package lists, and version
  choices with definitions from the main repository.
- [ ] Keep fleet-only packages and inputs inside =agent-fleet=.
- [ ] Remove definitions that no longer own behavior.
- [ ] Commit each independent change with its checks and document update.

Completion checks:

- Shared packages resolve from one definition.
- Fleet-only package behavior does not change.
- The affected local and fleet configurations evaluate after every commit.

** TODO 8. Deduplicate shared NixOS modules

- [ ] Reuse confirmed shared definitions from the main repository for Nix,
  caches, users, SSH, networking, Tailscale, Syncthing, and other common system
  behavior.
- [ ] Keep hardware, disk layout, cloud boot, and fleet access rules local to
  the fleet setup.
- [ ] Commit each coherent module change separately.

Completion checks:

- Each shared system rule has one source of truth.
- Fleet hosts retain their required cloud and headless behavior.
- All affected NixOS configurations evaluate after every commit.

** TODO 9. Deduplicate shared Home Manager and AI-agent setup

- [ ] Reuse confirmed shared definitions from the main repository for shell,
  development, editor, and AI-agent behavior.
- [ ] Remove copied Moshi, CLIProxyAPI, agent-files, and hook logic when the
  main definition is an exact replacement.
- [ ] Keep fleet-only credentials, services, and headless settings local.
- [ ] Commit each coherent behavior change separately.

Completion checks:

- Shared user and AI-agent behavior has one source of truth.
- Fleet-only behavior remains explicit and local.
- Home Manager evaluates for desktop and fleet users after every commit.

** TODO 10. Simplify paths and operational commands

- [ ] Update commands and references that still point to the old checkout.
- [ ] Make all agent-fleet recipes work from
  =/etc/nixos/agent-fleet=.
- [ ] Update sync and deployment source paths to include reused definitions
  without copying unrelated secrets to fleet hosts.
- [ ] Remove obsolete compatibility code and duplicate files after all callers
  use the shared owner.
- [ ] Commit each independent path or command change separately.

Completion checks:

- No active command depends on
  =/home/robert/projects/ai/agent-fleet=.
- Deployment inputs contain only the files needed by the fleet.
- Terraform state and credentials remain local and ignored.

** TODO 11. Run final checks and finish the move

- [ ] Run the root repository checks that cover changed shared definitions.
- [ ] Run the full agent-fleet =just check= recipe.
- [ ] Verify every local NixOS and macOS configuration affected by shared code.
- [ ] Verify every agent-fleet host configuration.
- [ ] Review the final Git status, history graph, ignored files, and commit
  boundaries.
- [ ] Confirm that no private key, environment file, Terraform state, cache,
  or unrelated destination file entered a commit.
- [ ] Record final results and any accepted limits in this document.
- [ ] Apply the confirmed old-checkout policy.

Completion checks:

- The integrated repository contains the full approved agent-fleet setup and
  history under =agent-fleet/=.
- Shared behavior has one owner in the main NixOS definitions.
- Each deduplication change is a separate, checked commit.
- The worktrees contain no unexpected tracked or untracked changes.
