#+TITLE: Integrate the agent fleet into the NixOS repository
#+DATE: [2026-09-04 Fri]
#+TODO: TODO WAIT | DONE CANCEL

* Goal

Move the complete agent-fleet setup into =/etc/nixos/agent-fleet=. Preserve its
reachable Git history. Make the main NixOS definitions the source of truth for
behavior that both setups share.

Keep the Terraform configuration and current local runtime state. Do not apply
Terraform changes, deploy a host, or switch a local system as part of this
work.

* Starting point

- The destination is =/etc/nixos= on branch =main=. It was seven commits ahead
  of =origin/main= at the first inspection. The remote advanced during the
  initial plan commit, so it is now one commit ahead.
- The destination has two unrelated untracked files:
  =remote-need-airy.tsv= and =remote-need-airy.txt=. This work must not stage,
  edit, or remove them.
- The source is =/home/robert/projects/ai/agent-fleet= on branch =main= at
  commit =94eaad3=. It has 47 reachable commits, no tags, and no configured
  remote.
- The source has no tracked changes. Its ignored local state includes
  =.direnv=, =.env=, =.terraform=, =known_hosts=, =result=, and Terraform state
  files.
- The ignored =.terraform= provider cache is about 25 MB. It will remain local
  and untracked after the move.
- The source tracks =ssh/id_ed25519= and =nix/agent-files-r2.env=. They contain
  a private key and R2 credentials. The source stores both files in plaintext.

* Working rules

- Make the initial plan its own commit.
- Import history from a temporary clone. Do not rewrite either existing
  repository in place.
- Preserve source commit authors, dates, messages, and topology. Record the old
  and filtered head commits and compare the reachable commit counts.
- Keep ignored local files ignored. Preserve their paths, file modes, and
  symlinks without reading or printing secret values.
- Use one focused commit for each deduplication change. Include the matching
  progress update in this document in that commit when practical.
- Mark a task =DONE= only after its checks pass. Record failed checks and other
  issues under the task where they occurred.
- Use the existing =just= recipes and Nix development shell for checks.
- Do not stage or modify unrelated work in the destination.
- Keep the old checkout until the integrated setup passes all checks. Removing
  it is a separate destructive action.

* Plan

** DONE 1. Confirm the secret and old-checkout policy
CLOSED: [2026-09-04 Fri 08:41]

- [X] Remove =ssh/id_ed25519= from every imported commit.
- [X] Move the current key to =shared/secrets/agent-fleet-ssh.key= and replace
  all private-key usage with that path.
- [X] Let the existing =*.key= Git attribute encrypt the moved key.
- [X] Remove =nix/agent-files-r2.env= from every imported commit and reuse
  =shared/secrets/agent-files-r2.env=.
- [X] Keep ignored secret-bearing state local and untracked.
- [X] Retain the old checkout after validation unless the user asks to remove
  it.

The Gitleaks history scan found the private key and two R2 credential fields.
It found no other secret in the 47 reachable commits. The ignored =.env= holds
the Hetzner token and an SSH-related value. The ignored Terraform state holds
resource IDs and public addresses. The integration will copy these ignored
files without adding them to Git.

** DONE 2. Record a reproducible import baseline
CLOSED: [2026-09-04 Fri 08:41]

- [X] Record the source and destination head commits, branches, worktree
  states, remotes, tags, and reachable commit counts.
- [X] Record tracked source paths and ignored local paths without reading
  secrets.
- [X] Check references to the old absolute source path so later path changes
  have a complete owner list.
- [X] Note any change from the starting point before the import starts.

Baseline:

- Destination head: =cb09fd5d97ee08ceef2cc4cb9976c685942217e0=.
- Destination tree: =981057f9f5bc63f222590b3e446fe6094ccd0d1e=.
- Destination =origin/main=: =8e4d22a942e033114379e14b8bc65bbfdb10399e=.
- Destination history: 605 commits. Branch =main= is one commit ahead of its
  remote.
- Source head: =94eaad34053a11485f113687673c1e8e9efad37d=.
- Source tree: =107fd6fecb6811d1983e4104318abc40b391931a=.
- Source history: 47 commits from root
  =306bf48a91118dd31f75e7d930ca051d26af1aad=. It has no tags or remotes.
- Source commit metadata signature:
  =7a005f88bf0f299ff87c1dfb73543472ea7a18916bd5d47df27232b1e84bfca4=.
- Source topology signature:
  =5af46e337558ba2f36349707d74fce0b498c4351205a9de46a31dd3f547b5520=.
- Approved source tree signature after secret removal:
  =5d812090c3fdba0f86bea617c4aa800531c0afb55e31d7d3e638d68e49f7ae72=.
  It covers 24 paths.
- Ignored local entries: =.direnv=, =.env=, =.terraform=, =known_hosts=,
  =result=, =terraform.tfstate=, and =terraform.tfstate.backup=.
- The old absolute path occurs in the Moshi copy note, the Codex trusted-project
  list, and the agent-fleet update instructions.

During the plan commit, another process pushed the seven earlier local commits
and advanced =origin/main= from =184c7c8= to =8e4d22a=. No file in the worktree
changed.

Completion checks:

- =remote-need-airy.tsv= remains untracked with SHA-256
  =0e316a71b921351d92ffb7684b3eedcf39e00df04985c1901bc7fb265299e696=.
- =remote-need-airy.txt= remains untracked with SHA-256
  =e3166c9cfee1612a46901845621dfc2259f175ebc0c88f8e29d2e98a088c3b7b=.
- The source worktree remains unchanged.
- The baseline provides enough detail to verify the filtered result.

** DONE 3. Rewrite a disposable copy of the agent-fleet history
CLOSED: [2026-09-04 Fri 08:42]

- [X] Clone the source into a temporary directory on the same machine.
- [X] Run =git filter-repo= there to prefix every source path with
  =agent-fleet/=.
- [X] Remove both confirmed secret files from history in the same disposable
  repository.
- [X] Keep the filter-repo commit map until the import checks pass.
- [X] Compare commit count, topology, authors, dates, messages, and the current
  tracked tree with the source baseline.

Results:

- Filtered head: =3c5e7a5d8b3c4f890ac687ea7f105786c563ca54=.
- Filtered tree: =8877650d9187cae1290f64bdbfa3ab69f9a59b82=.
- The commit map contains all 47 source commits. The filtered history has one
  root and no tags, like the source.
- The metadata, topology, and approved tree signatures match the baseline.
- All 24 approved paths start with =agent-fleet/=.
- Neither removed secret path occurs in the filtered history.
- Gitleaks scanned the 47 filtered commits and found no secret.
- =git fsck --full --strict= passes.

Completion checks:

- All expected source commits remain reachable from the filtered branch.
- Every tracked path is under =agent-fleet/=.
- The filtered current tree matches the approved 24-path source tree.
- The original source remains clean at
  =94eaad34053a11485f113687673c1e8e9efad37d=.

** DONE 4. Merge the filtered history and restore local state
CLOSED: [2026-09-04 Fri 08:45]

- [X] Fetch the filtered branch into =/etc/nixos= under a temporary local ref.
- [X] Merge it into =main= with unrelated histories allowed.
- [X] Copy the source worktree into =/etc/nixos/agent-fleet= without its =.git=
  directory.
- [X] Preserve all ignored local state, including =.terraform=, Terraform state,
  =.env=, =.direnv=, =known_hosts=, and =result=.
- [X] Copy the private key to =shared/secrets/agent-fleet-ssh.key=, replace its
  old uses, and add it through git-crypt.
- [X] Reuse =shared/secrets/agent-files-r2.env= and replace its fleet use.
- [X] Ignore the two removed source paths to prevent accidental reintroduction.
- [X] Verify that Git still ignores local files and that staging contains no
  secret or generated file.
- [X] Update this task before creating the merge commit.

Results:

- Local branch =import-agent-fleet= points to filtered head =3c5e7a5=.
- The pending merge has =1d8c93a= and =3c5e7a5= as its parents.
- Byte and directory comparisons confirm that all ignored local state matches
  the source. The =result= symlink also has the same target.
- The moved private key matches the source and has local mode =0600=.
- The staged private-key blob and the existing R2 blob have the git-crypt
  header.
- The sync recipe now copies the R2 credentials and Cachix netrc to the remote
  =shared/secrets= directory with mode =0600=.
- Git ignores both removed source paths. No removed secret remains in
  =agent-fleet=.

Completion checks:

- =git log= can traverse both the original NixOS history and the filtered
  agent-fleet history.
- The integrated tracked tree matches the approved filtered tree.
- The integrated local tree contains the ignored source files with the same
  modes and symlink targets.
- The earlier untracked destination files remain unchanged.

** DONE 5. Validate the unchanged integrated agent fleet
CLOSED: [2026-09-04 Fri 08:45]

- [X] Enter the agent-fleet development shell from its new path.
- [X] Run =just fmt= only when a format check requires changes.
- [X] Run =just check= from =/etc/nixos/agent-fleet=.
- [X] Check that the OpenTofu commands can use the copied local state and
  provider cache without applying a plan.
- [X] Check that each host configuration still evaluates from the new path.
- [X] Check operational recipes for stale absolute or relative paths.

Results:

- =nix develop ./nix --command just check= passes from the new directory.
- OpenTofu formatting and validation pass with the copied provider cache.
- =tofu show -json= reads the copied local state without changing it.
- The =agent-1= NixOS configuration, development shell, and formatter evaluate.
- The new private-key path resolves to the git-crypt-managed file.
- No old absolute checkout path remains inside =agent-fleet=.
- The format check found no change, so =just fmt= was not needed.
- Vale and Org lint pass for the changed documentation. Nixpkgs has no Fish
  Babel backend, so Org loaded the shell backend and did not execute the Fish
  example.

Completion checks:

- OpenTofu formatting and validation pass.
- Every agent-fleet NixOS configuration evaluates.
- The checks do not change infrastructure, hosts, or the local system.

** DONE 6. Inventory duplicated behavior and choose one owner for each rule
CLOSED: [2026-09-04 Fri 08:50]

- [X] Compare flake inputs, overlays, package sources, and lock-file ownership.
- [X] Compare core Nix and NixOS settings, caches, users, SSH, Tailscale, and
  Syncthing.
- [X] Compare Home Manager settings, development tools, shells, editors, and AI
  agent setup.
- [X] Compare Moshi, CLIProxyAPI, agent-files, and other local modules or
  scripts with the main repository.
- [X] Distinguish identical rules from machine-specific differences.
- [X] Add one concrete TODO task for each proven group of duplicated rules.
- [X] Commit the inventory before changing shared behavior.

Findings:

- The root and fleet flakes repeat ten inputs. Most inputs have the same locked
  revision. Home Manager and Flux Reconciler have drifted. The root flake and
  lock file will own all shared inputs and package overlays. Disko remains a
  fleet input in the root flake.
- =agent-fleet/nix/agent-files.nix= differs from
  =shared/home/agent-files.nix= only by a copy warning. The shared Home Manager
  module is an exact replacement.
- The fleet has a copied Moshi package, release file, update script, and NixOS
  service. The release and update files are identical. The package expression
  differs only in formatting. The shared service lacks the Grok setup required
  by the fleet.
  The shared Moshi module and =custom/moshi-hook= will own this behavior. The
  fleet-only remote pairing script will remain under =agent-fleet=.
- Both CLIProxyAPI modules build the same Codex and Grok wrappers and user
  service. The fleet also proxies Claude. The base Home Manager module currently
  keeps Claude outside the proxy. The fleet will use that base behavior.
- Both Syncthing modules repeat device IDs and folder membership. The base
  module has the current peers, selectable folders, and GUI authentication. It
  will own those rules. The fleet will keep its data path, open ports, selected
  folders, and Tailscale Serve service.
- The root and fleet repeat unfree-package policy, trusted Nix users, flake
  settings, store optimization, and two binary caches. A focused shared Nix
  module will own these rules. CUDA cache policy and fleet garbage collection
  remain local.
- Both Home Manager setups link the same configuration files and repeat Bash,
  Fish, Direnv, Git, GitHub CLI, and nix-index settings. A shared home module
  will own that command-line environment. Desktop-only packages and the minimal
  fleet Emacs setup remain separate.
- =agent-fleet/nix/ssh-key.pub= and =agent-fleet/ssh/id_ed25519.pub= are
  identical. The copy that Terraform uses under =ssh= will remain. NixOS will
  use it too.
- The fleet package list and the desktop package profiles overlap, but they
  serve different host roles. A shared lock and overlays remove version drift.
  Combining the package lists would pull graphical and hardware tools onto the
  headless host, so the lists will remain separate.
- Terraform, the Disko layout, the QEMU profile, cloud boot, the firewall,
  root-SSH recovery, browser-based Tailscale enrollment, headless packages,
  minimal Emacs setup, and remote operations are intentional differences.
- The base monolithic Linux and Home Manager modules include desktop behavior.
  The fleet will import focused shared modules instead of those broad modules.

The old absolute checkout path also occurs in a Codex trusted-project entry and
the external update-systems skill. Codex already trusts =/etc/nixos=, and its
configuration repository has unrelated changes, so this work will not edit that
stale entry. The update-systems instruction is an active command and must move
to the new path.

Completion checks:

- Every proposed deletion has a named replacement in the main repository.
- Every intentional difference has a reason and one owner.
- The inventory contains no speculative abstraction or unrelated cleanup.

** DONE 7. Reuse the shared agent-files module
CLOSED: [2026-09-04 Fri 08:52]

- [X] Import =shared/home/agent-files.nix= from the fleet Home Manager module.
- [X] Delete the copied fleet module.
- [X] Confirm that the service still uses the encrypted shared R2 file.
- [X] Evaluate the fleet Home Manager configuration and commit the change.

=just check= passes. The evaluated service uses
=/etc/nixos/shared/secrets/agent-files-r2.env=.

** DONE 8. Reuse the shared Moshi package and service
CLOSED: [2026-09-04 Fri 08:54]

- [X] Add the required fleet Grok setup to =shared/linux/moshi.nix=.
- [X] Import the shared Moshi module from the fleet NixOS configuration.
- [X] Delete the copied package, release, and update files.
- [X] Keep the remote pairing script as a fleet operation and update its path.
- [X] Make the fleet update recipe call the root Moshi update recipe.
- [X] Evaluate the desktop and fleet NixOS configurations and commit the
  change.

The shared Moshi service now installs the Claude, Codex, and Grok integrations.
The fleet remote-pairing helper moved to =agent-fleet/scripts/pair-moshi=. Its
update recipe delegates to the root recipe. Fleet =just check= and the root
flake check pass. The root check still reports the existing Tuxedo option rename
warning.

** DONE 9. Use one flake and lock file
CLOSED: [2026-09-04 Fri 08:56]

- [X] Add Disko and the fleet host outputs to the root flake.
- [X] Add the fleet control-plane development shell to the root flake.
- [X] Apply the root package overlays to fleet hosts.
- [X] Update fleet commands and =.envrc= to use the root flake.
- [X] Remove the nested fleet flake and lock file.
- [X] Evaluate all Linux and fleet hosts and commit the change.

The root flake now provides =storm=, =tuxedo=, and =agent-1= through one host
builder. It also provides the =agent-fleet= and =cuda= development shells. The
root lock added the same Disko revision used by the removed fleet lock. No other
root input changed. Fleet hosts now use the root Home Manager version, Flux
Reconciler version, overlays, and shared Home Manager wiring.

=nix develop ..#agent-fleet --command just check= passes from =agent-fleet=.
The command checks OpenTofu and all three Linux NixOS configurations. The
existing Tuxedo rename warning remains. Dry runs confirm that build, install,
switch, and update recipes all use the root flake and lock file.

** DONE 10. Reuse the shared CLIProxyAPI Home Manager module
CLOSED: [2026-09-04 Fri 08:58]

- [X] Import =shared/linux-home/cli-proxy-api.nix= from the fleet Home Manager
  module.
- [X] Make that module install every command that its service and wrappers need.
- [X] Remove its duplicate package from the broad desktop package list.
- [X] Delete the fleet NixOS copy and its import.
- [X] Confirm that fleet behavior follows the base Claude routing choice.
- [X] Evaluate desktop and fleet Home Manager configurations and commit the
  change.

The shared Home Manager module now owns the CLIProxyAPI executable, service,
Codex wrapper, Grok wrapper, and direct Claude package. Evaluated package and
service checks match on =agent-1= and =storm=. The fleet no longer defines a
NixOS-level copy. The full check passes.

The first check command used a =just --working-directory= form that this Just
version rejects. It stopped before running a recipe or changing state. Running
the check from =agent-fleet= worked.

** TODO 11. Reuse the shared Syncthing inventory

- [ ] Make the shared module omit the current machine from its peer and folder
  lists.
- [ ] Import the shared module from the fleet wrapper.
- [ ] Select the four fleet folders through the shared options.
- [ ] Keep the fleet data path, firewall ports, and Tailscale Serve service in
  the fleet wrapper.
- [ ] Copy only the shared Syncthing password needed by fleet hosts.
- [ ] Evaluate desktop and fleet Syncthing settings and commit the change.

** TODO 12. Reuse core Nix settings

- [ ] Move common Nix settings from =shared/linux/default.nix= into one focused
  shared module.
- [ ] Import that module from the desktop base and fleet configuration.
- [ ] Keep the CUDA cache in the desktop base.
- [ ] Keep fleet garbage collection and any fleet-only trust setting local.
- [ ] Evaluate desktop and fleet Nix settings and commit the change.

** TODO 13. Reuse the command-line Home Manager setup

- [ ] Move shared configuration links and command-line program settings into a
  focused =shared/home= module.
- [ ] Import it from the Linux desktop and fleet Home Manager modules.
- [ ] Remove the replaced fleet definitions.
- [ ] Keep desktop-only tools and fleet-only Home Manager settings local.
- [ ] Evaluate desktop and fleet Home Manager configurations and commit the
  change.

** TODO 14. Deduplicate paths and deployment inputs

- [ ] Make NixOS use the Terraform public-key file and delete its duplicate.
- [ ] Update every agent-fleet flake path and old-checkout command.
- [ ] Copy only the root flake files, selected shared modules, fleet modules,
  and required secrets to fleet hosts.
- [ ] Update the external update-systems instruction in its own repository.
- [ ] Confirm that no active command uses the old checkout and commit each
  repository change separately.

** TODO 15. Run final checks and finish the move

- [ ] Run the root repository checks that cover changed shared definitions.
- [ ] Run the full agent-fleet =just check= recipe.
- [ ] Verify every local NixOS and macOS configuration affected by shared code.
- [ ] Verify every agent-fleet host configuration.
- [ ] Review the final Git status, history graph, ignored files, and commit
  boundaries.
- [ ] Confirm that no private key, environment file, Terraform state, cache,
  or unrelated destination file entered a commit.
- [ ] Record final results and any accepted limits in this document.
- [ ] Retain the old checkout as agreed.

Completion checks:

- The integrated repository contains the full approved agent-fleet setup and
  history under =agent-fleet/=.
- Shared behavior has one owner in the main NixOS definitions.
- Each deduplication change is a separate, checked commit.
- The worktrees contain no unexpected tracked or untracked changes.
